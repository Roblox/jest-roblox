# jest-runner

Upstream: https://github.com/facebook/jest/tree/v28.0.0/packages/jest-runner

Executes test files. Takes a list of tests from jest-core, runs each through the test framework (jest-circus), collects results, and emits events for reporters. Supports serial execution; parallel workers are not implemented in Roblox.
